[Login]
# Lock screen when lid is closed on battery
# NOTE: AMD Renoir laptops have broken s2idle suspend - use lock with aggressive TLP settings
# For other systems, consider changing to suspend if it works properly
HandleLidSwitch=lock
HandleLidSwitchExternalPower=suspend
HandleLidSwitchDocked=ignore

# Respect inhibitors for lid switch (allows apps to prevent suspend when needed)
LidSwitchIgnoreInhibited=no

# Disable systemd idle suspend - let hypridle handle idle management instead
# This prevents conflicts and ensures clean suspend/resume behavior
IdleAction=ignore
