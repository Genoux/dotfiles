# Copy to service.env in this directory. Read by the 9router user service and
# passed into the container; keep it out of git, it is a live password.
#
# The dashboard refuses its own 123456 default for anything it considers a
# remote login, and behind Docker's bridge every request looks remote.

# INITIAL_PASSWORD=choose-something-long
