# Hyprland theme colors (Generated by flavours from default)
# Only colors - appearance settings are in appearance.conf

general {
  # Border colors based on theme
  col.active_border = rgba(220,220,220,0.3)
  col.inactive_border = rgba(74,74,74,0.2)
}

decoration {
  # Shadow color based on theme
  shadow {
    color = rgba(22,22,27,0.5)
  }
}