# ================================
# HYPRIDLE CONFIG
# ================================
# Minimal config for bluetooth reconnection on resume only

# ================================
# GENERAL SETTINGS
# ================================
general {
    after_sleep_cmd = ~/.local/bin/system-bluetooth
    ignore_dbus_inhibit = false
    ignore_systemd_inhibit = false
}
